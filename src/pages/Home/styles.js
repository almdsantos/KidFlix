import {StyleSheet} from 'react-native';

const styles = StyleSheet.create ({
    container: {
        flex: 1,
        backgroundColor: '#000',
    },
    previaContainer: {
        width: '100%',
        marginTop: 10,
    },
    previaTitle: {
        textAlign: 'center'
    },
});

export default styles;